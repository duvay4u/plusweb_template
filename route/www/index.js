///////////////////////////////////////////////////////////////////////////////////////////////////////////////
// route > www > index.js
//
///////////////////////////////////////////////////////////////////////////////////////////////////////////////


const router = require( 'express' ).Router();


// GET : home
router.get( '/', ( req, res, next ) => {

	res.render( './www/index', { title: 'Express' } );
});


module.exports = router;
