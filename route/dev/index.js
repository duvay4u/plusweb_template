///////////////////////////////////////////////////////////////////////////////////////////////////////////////
// route > dev > index.js
//
///////////////////////////////////////////////////////////////////////////////////////////////////////////////


const router = require( 'express' ).Router();


// GET : home
router.get( '*', ( req, res, next ) => {

	res.send( 'dev.localhost.net - sub doamin' );
});


module.exports = router;
